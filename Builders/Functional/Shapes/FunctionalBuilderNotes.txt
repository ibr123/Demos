* Functional Builder Pattern — Summary
  (concept from Dmitri Nesteruk, "Design Patterns in C# and .NET", Udemy;
   examples below use THIS repo's Shape implementation)

	What it is:
	- A builder whose state is NOT a half-built object, but a LIST OF FUNCTIONS
	  (List<Action<Shape>>) — a recipe of deferred mutations.
	- Each step (.DefineShape, .CalculateCircleArea, ...) only RECORDS an action;
	  nothing is built yet.
	- Build() creates a fresh Shape and replays every recorded action onto it.

	The chain:
	  .DefineShape("Circle")        -> ShapeBuildingActions += (s => s.ShapeType = "Circle")
	  .CalculateCircleArea(5)       -> ShapeBuildingActions += (s => s.Area = π·5²)
	  .CalculateCirclePerimeter(5)  -> ShapeBuildingActions += (s => s.Perimeter = 2·π·5)
	  .Build()                      -> s = new Shape(); run all actions; clear; return s


* THE WHOLE POINT: Open/Closed Principle

	- ShapeBuilder only defines the core step `DefineShape`.
	  (Both the course AND this repo SEAL the builder: `public sealed class ShapeBuilder`.
	   See the "WHY SEALED" note below for why intent — not safety/perf — is the reason.)
	- CalculateCircleArea / CalculateCirclePerimeter (and the Triangle ones) are
	  added as EXTENSION METHODS in separate static plugin classes (CirclePlugin,
	  TrianglePlugin), adding NEW build steps WITHOUT modifying ShapeBuilder.
	- "Open for extension, closed for modification."
	- Classic builder: every new step = edit the builder class.
	  Functional builder: new step = just write an extension method (a new plugin).

	- THE TEST (proves OCP is the design DRIVER, not just a side benefit):
	    "If I delete the OCP benefit, does the functional design still make sense?"
	    - No. If you didn't care about third parties adding steps without
	      touching your class, you'd use a CLASSIC builder — simpler, better
	      encapsulated, and the List<Action<T>> machinery buys you nothing.
	    - The list-of-functions approach only EARNS its keep because it lets
	      extension methods plug in new steps.
	    - So every structural choice (public ShapeBuildingActions, deferred
	      Build()) is bent toward making OCP extensibility possible.
	- CAVEAT: OCP is the reason behind THIS VARIANT, not the Builder pattern
	  as a whole. Builders in general exist to construct complex objects
	  step-by-step; only the functional variant is driven by OCP.


* WHY SEALED (ShapeBuilder is `public sealed class`)

	- The ONLY load-bearing reason here is DESIGN INTENT, not safety or perf:
	  "extend me via EXTENSION METHODS from the outside, never by subclassing."
	  Sealing enforces that contract instead of just implying it.
	- The usual textbook reasons DON'T apply to this class:
	    - "prevents overriding / breaking invariants" -> there are NO virtual members,
	      so a subclass couldn't override anything anyway. (It could only HIDE a method
	      with `new`, which doesn't change behavior for callers holding a ShapeBuilder.)
	    - "devirtualization perf" -> only helps virtual call sites; there are none.
	- So seal it purely as a COMMUNICATED CONVENTION: new steps arrive as
	  CirclePlugin-style extension methods, not via inheritance.
	- The plugins (CirclePlugin/TrianglePlugin) are `static` classes = ALREADY
	  implicitly sealed; you can't write `sealed` on them and can't inherit them.
	- Rule of thumb: seal by default UNLESS a class is explicitly designed as a base
	  class. For a builder whose extension point is extension methods, sealed fits.


* HOW IT WORKS (mechanics)

	- ShapeBuildingActions is `public readonly List<Action<Shape>>`.
	    - readonly  = can't REASSIGN the list reference.
	    - public    = plugin extension methods can ADD to it (needed for extensibility).
	    - contents are still mutable -> the trade-off: less encapsulation.
	- DefineShape(shapeType): ShapeBuildingActions.Add(s => { s.ShapeType = shapeType; }); return this;
	    - the lambda CAPTURES `shapeType` in a closure, remembered until Build().
	- CalculateCircleArea(radius): ShapeBuildingActions.Add(s => { s.Area = π·radius²; }); return shapeBuilder;
	    - same recipe, but contributed from a plugin via `this ShapeBuilder` extension method.
	- Build(): var s = new Shape(); ShapeBuildingActions.ForEach(a => a(s)); ShapeBuildingActions.Clear(); return s;
	    - all deferred work runs HERE, in insertion order; Clear() resets the recipe.
	- `return this` / `return shapeBuilder` on each step enables fluent chaining.


* SEQUENCE (full trace: from `new Shape()` to invocation, building the Circle)

	Step 0 — Build() creates the object:
	    var s = new Shape();
	    -> s.ShapeType = null, s.Area = 0, s.Perimeter = 0   (empty shell, nothing applied yet)

	Step 1 — replay the recorded actions (ShapeBuildingActions.ForEach(a => a(s))):
	    Actions[0]  is   s => { s.ShapeType = shapeType; }   // from .DefineShape("Circle")
	    Actions[1]  is   s => { s.Area = π·radius²; }         // from .CalculateCircleArea(5)
	    Actions[2]  is   s => { s.Perimeter = 2·π·radius; }   // from .CalculateCirclePerimeter(5)

	Step 2 — first invocation:  a(s)  where a == Actions[0]
	    runs  s.ShapeType = "Circle";    // closure replays captured shapeType
	    -> ShapeType="Circle", Area=0, Perimeter=0

	Step 3 — second invocation: a(s)  where a == Actions[1]
	    runs  s.Area = π·5²;             // closure replays captured radius
	    -> ShapeType="Circle", Area=78.54, Perimeter=0

	Step 4 — third invocation:  a(s)  where a == Actions[2]
	    runs  s.Perimeter = 2·π·5;       // closure replays captured radius
	    -> ShapeType="Circle", Area=78.54, Perimeter=31.42

	Step 5 — Build() clears the recipe and returns the now-complete s:
	    ShapeBuildingActions.Clear(); return s;
	    // => Shape { ShapeType="Circle", Area=78.54, Perimeter=31.42 }

	Key timing note:
	    - Step 1's lambdas were RECORDED earlier, back during the chain
	      (.DefineShape / .CalculateCircleArea / ...), but they only RUN now,
	      in Build(), in order.
	    - `a(s)` = invoke the stored Action<Shape> with this exact s.
	    - Same field set twice? Last invocation in the sequence wins.


* "FUNCTIONAL" — why the name?

	- Builder state = a list of functions, not object state.
	- Building = function application (apply each Action<Shape> to the object).
	- That FP flavor is where "functional builder" comes from.


* CLASSIC vs FUNCTIONAL (quick table)

	| Aspect            | Classic Builder        | Functional Builder                  |
	|-------------------|------------------------|-------------------------------------|
	| Builder state     | half-built object      | List<Action<T>>                     |
	| Mutation timing   | immediately, per call  | all at once, in Build()             |
	| Add a new step    | edit the builder class | write an extension method (plugin)  |
	| Encapsulation     | strong (state hidden)  | weaker (ShapeBuildingActions public)|
	| Best for          | fixed set of steps     | steps third parties extend          |


* GOTCHAS / NOTES

	- Deferred execution: actions run in the order added; if two set the same
	  field, the LAST one added wins.
	- Closures capture the value passed at call time and replay it unchanged.
	- Repetition smell: every step is `ShapeBuildingActions.Add(...); return ...;`.
	  -> Next lecture: GENERIC functional builder (recursive generic / CRTP)
	     with a single Do(...) helper to remove the boilerplate.
	- Program.cs builds the shapes but never prints them. Add e.g.:
	     Console.WriteLine($"{circle.ShapeType}: area={circle.Area}, perimeter={circle.Perimeter}");
	     // Circle: area=78.5398..., perimeter=31.4159...


* COMPILE ERROR — double -> float (CS1503) when calling a plugin

	The error:
	    Program.cs(14,71): error CS1503: Argument 2: cannot convert from 'double' to 'float'
	    // from:  shapeBuilder.DefineShape("Circle").CalculateCircleArea(5.3)...

	Cause — LITERAL TYPING:
	- A bare decimal literal like `5.3` (or `5.4`) is a DOUBLE by default.
	- `5.3f` (or `5.3F`) is a FLOAT — the `f`/`F` suffix picks the type.
	- The plugin signature wants a float:
	      public static ShapeBuilder CalculateCircleArea(this ShapeBuilder shapeBuilder, float radius)
	- So passing the double `5.3` into a float parameter is rejected.

	Why "Argument 2" (not Argument 1):
	- It's an EXTENSION METHOD. `shapeBuilder.CalculateCircleArea(5.3)` is sugar for
	  `CirclePlugin.CalculateCircleArea(shapeBuilder, 5.3)`.
	  -> arg 1 = the receiver `shapeBuilder`, arg 2 = `5.3` (the radius). Hence "Argument 2".

	Why the compiler refuses (NARROWING vs WIDENING):
	- double -> float is a NARROWING conversion (double holds more range/precision than
	  float), so C# will NOT do it implicitly — you could silently lose data.
	- The reverse, float -> double, is WIDENING and happens implicitly with no error.
	- That's also why `.CalculateCirclePerimeter(5)` is fine: `5` is an INT, and
	  int -> float is widening (implicit). Only the decimal literal trips it.

	Fixes (any one):
	  (a) `CalculateCircleArea(5.3f)`     — make it a float literal (simplest).
	  (b) `CalculateCircleArea((float)5.3)` — explicit cast (also narrows; you opt in).
	  (c) change the parameter to `double radius` in the plugin — if you'd rather the
	      API accept doubles. (Then `5.3` works as-is.)

	Literal-suffix cheat sheet:
	    5      -> int
	    5.4    -> double        (default for decimals)
	    5.4f   -> float
	    5.4m   -> decimal       (high precision; money)
	    5L     -> long
	    5.0 / 5d -> double


* RELATION TO OTHER BUILDER VARIANTS

	- Fluent builder   -> returns `this` for chaining (this file does that too).
	- Stepwise builder -> enforces call ORDER via interfaces (e.g. the Chicken example).
	- Faceted builder  -> multiple sub-builders for different facets.
	- Functional builder (THIS) -> optimizes for EXTENSION without modification.
	  It enforces nothing about order; its strength is open/closed extensibility.


* PROBLEM 1 — REUSED BUILDER ACCUMULATES ACTIONS (the Shape example)

	Symptom:
	- One builder instance is reused to build two products:
	    ShapeBuilder b = new();
	    Shape circle   = b.DefineShape("Circle")...Build();
	    Shape triangle = b.DefineShape("Triangle")...Build();   // SAME b
	- If Build() does NOT clear ShapeBuildingActions, the list only grows.
	  The triangle's Build() then replays ALL SIX actions (circle's 3 + triangle's 3)
	  on its fresh Shape.

	Why it still looks correct:
	- Last-write-wins: the triangle's 3 actions run LAST and overwrite all three
	  fields the circle had set. Midway through Build() the object is briefly a
	  COMPLETE circle, then the triangle stamps over it.

	Where it breaks (silent bug):
	- If the triangle set Area but NOT Perimeter, the circle's leftover Perimeter
	  is never overwritten -> "triangle" silently reports the circle's perimeter.
	- Also wasted work: re-runs circle's actions every time; cost grows per product
	  (3 actions, then 6, then 9, ...).

	Root cause:
	- Builder state is a recipe that ACCUMULATES; without a reset, Build() only
	  READS it and leaves prior steps in place.

	Solutions (preferred -> fallback):
	  (a) BEST — a fresh builder per product. Eliminates shared state instead of
	      managing it. Match the Duck/Chicken style with a static entry point:
	          public static ShapeBuilder Builder => new();   // fresh recipe each time
	          Shape circle = Shape.Builder.DefineShape("Circle")...Build();
	  (b) FALLBACK — reset inside Build() when you MUST reuse one instance
	      (THIS is what the code currently does):
	          public Shape Build() {
	              Shape s = new();
	              ShapeBuildingActions.ForEach(a => a(s));
	              ShapeBuildingActions.Clear();   // next product starts clean
	              return s;
	          }
	      Downside: Build() now has a HIDDEN side effect (mutates the builder) —
	      surprising for a method that "just builds".

	Deep-understanding note:
	- Both fix the symptom. Fresh-instance ELIMINATES shared state (nothing to leak);
	  Clear() carefully MANAGES shared state (leak, then wipe). Removing a hazard
	  beats remembering to defuse it -> default to fresh.


* PROBLEM 2 — INJECTED BUILDER STAYS THE SAME INSTANCE (even when Transient)

	Symptom:
	- ShapeBuilder is injected and used to build two shapes in one class:
	    class ShapeService {
	        readonly ShapeBuilder _b;
	        ShapeService(ShapeBuilder b) => _b = b;   // resolved ONCE, stored in a field
	        ... _b.DefineShape("Circle")...Build();   // both builds share _b
	        ... _b.DefineShape("Triangle")...Build(); // -> Problem 1 is back
	    }
	- Registering AddTransient<ShapeBuilder>() does NOT help.

	Why Transient doesn't save you:
	- DI lifetime = how often the CONTAINER CREATES the instance, NOT how often you
	  reuse it.
	- Transient = "new per RESOLUTION". A constructor parameter is resolved exactly
	  ONCE (at construction), so you get one builder in a field and reuse it.
	- KEY: to get "new per USE" you must RESOLVE per use, not inject per construction.
	  No object lifetime (Transient/Scoped/Singleton) can give per-call freshness
	  through a plain injected field.

	The trap:
	- Picking Transient and thinking "new instance, so I'm safe" SHIPS the bug —
	  the safety you imagined (per-use) isn't the safety the lifetime provides
	  (per-resolution). The fix is not a different lifetime; it's changing WHAT you
	  inject so resolution happens per use.

	Solutions (preferred -> fallback):
	  (a) BEST — inject a FACTORY, not the builder. Every call resolves a fresh one:
	          class ShapeService {
	              readonly Func<ShapeBuilder> _make;
	              ShapeService(Func<ShapeBuilder> make) => _make = make;
	              ... _make().DefineShape("Circle")...Build();
	              ... _make().DefineShape("Triangle")...Build();
	          }
	      Registration (MS.Extensions.DI doesn't auto-make Func<T>):
	          services.AddTransient<ShapeBuilder>();
	          services.AddTransient<Func<ShapeBuilder>>(
	              sp => () => sp.GetRequiredService<ShapeBuilder>());
	      (Autofac and others provide Func<T> for free.)
	  (b) FALLBACK — Clear() in Build() (Problem 1's fallback) so reuse is safe
	      regardless of how the instance is shared.

	Design insight:
	- A builder is a SHORT-LIVED, SINGLE-USE, STATEFUL object. That is at odds with
	  being a long-lived injected dependency. So don't inject the builder directly
	  (that's the smell that created the question) — inject the ability to MAKE
	  builders (Func<ShapeBuilder> or a small IShapeBuilderFactory). The factory is
	  what's safe to be long-lived; each builder it hands out is fresh and single-use.
	- A static `Shape.Builder => new()` entry point also fixes Problem 1, but a
	  static new() can't be mocked/substituted, so it fights testability in a DI
	  codebase -> prefer the factory there.
